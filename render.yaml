services:
  - type: web
    name: mystical-gpt-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python gpt_query_api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
